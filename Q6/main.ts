// Store the person's name with whitespace characters

let personname: string = "\t Eric \n ";

// Print the name with the whitespace characters
console.log(personname);

// Strip the whitespace characters
let trimmed_name: string = personname.trim();

// Print the trimmed name
console.log(trimmed_name);