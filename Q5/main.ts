// Store the name of the famous person in a variable
let famous_person: string = "Albert Einstein";

// Compose the quote in a variable
let message: string = `"A person who never made a mistake never tried anything new." - ${famous_person}`;

// Print the message
console.log(message);